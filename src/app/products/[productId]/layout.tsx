
export default function ProductLayout({
    children,
}: {
    children: React.ReactNode
}) {
    return (
        <div>
            {children}
            < p > image carousel</p >
        </div>

    )
}
